# The new async project had no requirements.txt at all (verified during this
# migration pass — imports failed for bcrypt, email-validator, aiosqlite,
# python-jose). Rebuilt from the legacy project's pinned deps, swapping the
# sync DB driver for async equivalents.

fastapi==0.115.0
uvicorn==0.30.6
starlette==0.38.6

# ORM / DB (async)
SQLAlchemy==2.0.32
alembic==1.18.5
asyncpg==0.29.0          # async Postgres driver (replaces psycopg for runtime use)
psycopg[binary]==3.3.4   # kept for alembic's sync migration runner

# Validation / settings
pydantic==2.13.4
pydantic-settings==2.14.2
email_validator==2.2.0

# Auth / security
bcrypt==4.1.3
passlib==1.7.4
python-jose==3.3.0
python-multipart==0.0.9
cryptography==49.0.0

# Business logic utilities (ported from legacy — PDF/QR ID cards, fuzzy search)
qrcode==7.4.2
reportlab==4.2.5
pillow==12.2.0
rapidfuzz==3.14.5

# Misc
python-dotenv==1.0.1
Jinja2==3.1.6

# Dev tooling
black==26.5.1
isort==8.0.1
ruff==0.15.20
