[tool.ruff.lint.per-file-ignores]
# load_dotenv() must run before any module that reads env vars is imported,
# so these files deliberately place some imports below code.
"alembic/env.py" = ["E402"]
"scripts/*.py" = ["E402"]
"src/main.py" = ["E402"]
